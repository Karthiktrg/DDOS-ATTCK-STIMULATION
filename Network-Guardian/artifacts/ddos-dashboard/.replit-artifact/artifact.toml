kind = "web"
previewPath = "/"
title = "Network Traffic Analyzer"
version = "1.0.0"
id = "artifacts/ddos-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22075

[services.development]
run = "pnpm --filter @workspace/ddos-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ddos-dashboard", "run", "build" ]
publicDir = "artifacts/ddos-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22075"
BASE_PATH = "/"
